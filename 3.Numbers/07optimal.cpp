#include <bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cout<<"enter no.";
    cin>>n;
    if(n&1==0)cout<<"even no. hai";  // bitwise operations are faster than normal operations 
    else cout<<"odd no.";
}